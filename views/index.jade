extends layout

block content
    if showLogin
        div.row
            div.col-md-12.text-center
                h1 Photo Previewer
                    p Before you can mail a photo, need to authorize
                    a(href="/authorize-user" class="btn btn-primary") Login to Instagram
